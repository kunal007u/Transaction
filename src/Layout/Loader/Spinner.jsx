import React from 'react';
import './spinner.css';

const Spinner = () => {
    return (
        <div className="loader-wrapper">
            <span className="loader"></span>
        </div>
    );
};

export default Spinner;

